/*const schema = require("schm");

const serviceSchema = schema({
    id: Number,
    seller_id: Number,
    name: String,
    description: String,
    price: Number
});

module.exports = serviceSchema;*/
